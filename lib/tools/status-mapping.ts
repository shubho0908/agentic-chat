export function mapYouTubeStatus(status: string): string {
  return status;
}

export function mapDeepResearchStatus(status: string): string {
  return status;
}

export function mapGoogleSuiteStatus(status: string): string {
  return status;
}
